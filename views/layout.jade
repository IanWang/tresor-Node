!!!
html
  head
    title trésor
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width,initial-scale=1.0')
    link(rel='stylesheet', href='/css/bootstrap.css')
    link(rel='stylesheet', href='/css/bootstrap-responsive.css')
    link(rel='stylesheet', href='/css/colorbox.css')
    link(rel='stylesheet', href='/css/layout.css')
    script(src='//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js')
    script(src='/js/lib/bootstrap.min.js', type='text/javascript')
    script(src='//connect.facebook.net/en_US/all.js', type='text/javascript')
    script(src='/js/lib/jquery.colorbox.min.js')
    //-
      script.
        (function(d, s, id) {
        var js, fjs = d.getElementsByTagName(s)[0];
        if (d.getElementById(id)) return;
        js = d.createElement(s); js.id = id; js.async = true;
        js.src = "//connect.facebook.net/en_US/all.js#xfbml=1&appId=365913330182067";
        fjs.parentNode.insertBefore(js, fjs);
        }(document, 'script', 'facebook-jssdk'));
            
  body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container-fluid
          a.btn.btn-navbar(href="#", data-toggle="collapse", data-target=".nav-collapse")
            span.icon-th-list 
          a.brand(href='/')
            em trésor
          .nav-collapse.collapse
            ul.nav.pull-right
              li 
                a(href='/') 首頁
              li
                a(href='/user') 記錄
              li 
                a(href='/create') 新增
              li 
                a(href='/log_out') 登出
    block content
    #footer
      #feedbackForm
        form
          h3 問題回報與建議
          label(for='fbTitle') 標題
          input#fbTitle(type='text', required)
          label(for='fbContent') 內容
          textarea#fbContent(rows='6', cols='40', required)
          hr
          .btn.submit 提交
    #suggest
      a(href="#feedbackForm").feedback Feedback
  block scripts  
  script.
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
    (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
    m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');
    ga('create', 'UA-43441200-1', 'tresor.tw');
    ga('send', 'pageview');
  script.
    $(document).ready(function(){
      var current = window.location.href.split('/');
      var where = '/' + current[current.length-1];
      $('.nav li a').each(function(){
        var href = $(this).attr('href');
        if(where === href) {
          $('.nav li').removeClass("active");
          $(this).parent().addClass("active");
        }
      });
    });
    navigator.sayswho= (function(){
      var N= navigator.appName, i,
      ua= navigator.userAgent, tem,
      M= ua.match(/(opera|chrome|safari|firefox|msie)\/?\s*(\.?\d+(\.\d+)*)/i);
      if(M && (tem= ua.match(/version\/([\.\d]+)/i))!= null) M[2]= tem[1];
      M= M? [M[1], M[2]]: [N, navigator.appVersion,'-?'];
      var b = M[0],  v = M[1];
      var w = '<div class="top-warning"><div><h3>注意！！</h3><p>您所使用的瀏覽器過舊。可能導致某些功能無法正常運作</p><p>為享有最佳的體驗，我們強烈建議您使用<a href="http://www.google.com/intl/zh-TW/chrome/">Google Chrome</a>或是<a href="http://moztw.org/firefox/">Firefox</a></p><button class="btn btn-primary">我知道了</button></div></div>';
      if(b === 'MSIE' ) $('html').append(w);
    })();
    $('.top-warning button').click(function(){
      $('.top-warning').remove();
    });
  //-
    script.
      (function($){
        $(window).load(function(){
          $("#cboxLoadedContent").mCustomScrollbar({
            mouseWheel:true, 
            mouseWheelPixels:"auto",
            autoDraggerLength:true, 
            autoHideScrollbar:false, 
            scrollButtons:{ 
              enable:false,
              scrollSpeed:"auto", 
              scrollAmount:60 
            },
            advanced:{
              updateOnBrowserResize:true,
              autoExpandHorizontalScroll:false,
              autoScrollOnFocus:true,
            },
            theme:"light"
          });
        });
      })(jQuery);
              

